Sono presenti 2 script:

equilibration.py:
		testa la delta di Metropolis per stati 1s e 2p per distribuzione uniforme ("riciclata" anche per Gauss) partendo da una posizione 	
		iniziale ragionevole sia per 1s che 2p
		risultati confrontati a mano, scelgo parametri migliori per execute.py
		
execute.py:
		con delta trovate in precedenza eseguo il codice (sia uniforme che Gauss) per 1s e 2p, mentre solo uniforme per 3d e 4f.
